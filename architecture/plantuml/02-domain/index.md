---
title: Domain Model
description: Core domain entities and relationships for Movie Finder — identity, sessions, messaging, AI pipeline state.
---

# Domain Model

Core data structures and their relationships: identity (User), session management, conversation messaging, AI pipeline state, and movie candidate representation.

---

## 01 — Domain Model

All domain entities: `User`, `Session`, `Message`, `MovieCandidate`, `MovieFinderState`, `Phase` enum, and `RefreshToken`. Relationships show ownership and state dependency.

![Domain Model](01-domain-model.png)
